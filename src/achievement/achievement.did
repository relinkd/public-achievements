type Result = variant { Ok : bool; Err : text };
type Result_1 = variant { Ok : text; Err : text };
service : {
  caller : () -> (principal) query;
  checkAchievementEligibility : (principal, blob) -> (Result) query;
  receiveToIdentityWallet : (principal, blob) -> (Result_1);
}
